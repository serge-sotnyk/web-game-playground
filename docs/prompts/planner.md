<!--
Planner session prompt.

Substitute the placeholder before pasting:
  {{LETTER}}  →  the planner's anonymised letter, "a" or "b"

Paste EVERYTHING BELOW the divider into the session. Do not paste this comment.
The clone contains AGENTS.md and docs/brief.md, so the agent reads both itself.
Do not answer design questions during the session; the choice being its own is
the thing being measured. Every exchange beyond this prompt is an intervention —
log it.
-->

---

You are the **planner** for this round. Read `AGENTS.md` first, then the brief
in `docs/brief.md`.

Write your plan to `docs/plans/plan-{{LETTER}}.md`. Create nothing else — no
code, no scaffolding. Commit and push when you are done.
